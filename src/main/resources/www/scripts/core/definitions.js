/**
 * Main definitions for the whole project (a.k.a. "globals").
 */
var definitions = (function() {
    var defs = {};

    defs.getTheme = function() {
        return 'blue';
    };

    return defs;
})();
